{
  "version": 1,
  "note": "Global, pre-researched DKB seed corpus. Source=seed, scope=global. Loaded idempotently by saddle.seed. Extend offline by adding entries with new stable ids.",
  "entries": [
    {
      "id": "knowledge_seed_root_cause",
      "kind": "best_practice",
      "title": "Diagnose the root cause before designing",
      "body": "Trace every symptom to the underlying design problem and fix that cause, so the whole class of bug closes at once instead of the single instance in front of you. A fix that only addresses the surface report will let the same defect resurface elsewhere.",
      "tags": ["diagnosis", "root-cause", "thinking"]
    },
    {
      "id": "knowledge_seed_question_the_frame",
      "kind": "best_practice",
      "title": "Question whether the frame is even right",
      "body": "Before optimizing inside an approach, ask whether the approach itself is correct: is this the right structure, the right kit, the right model of the problem? Most shallow designs come from optimizing within a frame that should have been replaced.",
      "tags": ["reframe", "premise", "thinking", "high-level"]
    },
    {
      "id": "knowledge_seed_structural_over_patch",
      "kind": "best_practice",
      "title": "Prefer a structural fix over a patch",
      "body": "Redesign the mechanism so the failure cannot recur, rather than wrapping a guard around the symptom. Structural fixes shrink the system's defect surface; patches grow it.",
      "tags": ["structure", "design", "no-bandaid"]
    },
    {
      "id": "knowledge_seed_second_order",
      "kind": "best_practice",
      "title": "Reason about second-order effects",
      "body": "A change that resolves the immediate issue can create downstream ones. Design for the consequences of the fix across the whole system, not just the local symptom it removes.",
      "tags": ["second-order", "consequences", "systemic"]
    },
    {
      "id": "knowledge_seed_single_source_of_truth",
      "kind": "best_practice",
      "title": "Keep a single source of truth",
      "body": "Every fact lives in exactly one place and everything else derives from it, so values cannot drift out of sync. Duplicated state is a future inconsistency waiting to happen.",
      "tags": ["sot", "duplication", "consistency", "abstraction"]
    },
    {
      "id": "knowledge_seed_illegal_states",
      "kind": "best_practice",
      "title": "Make illegal states unrepresentable",
      "body": "Encode invariants in the types and schema so wrong combinations cannot be constructed at all, instead of validating after the fact and hoping every call site remembers to check.",
      "tags": ["invariants", "schema", "types", "correctness"]
    },
    {
      "id": "knowledge_seed_general_case",
      "kind": "best_practice",
      "title": "Design for the general case",
      "body": "Solve the whole class of problem (any tenant, any input, any genre), not the one example in front of you. A design that only works for the immediate instance is the wrong design.",
      "tags": ["generality", "abstraction", "agnostic"]
    },
    {
      "id": "knowledge_seed_fail_loud",
      "kind": "best_practice",
      "title": "Fail loud, never silent",
      "body": "Surface errors where they happen with enough context to act on them. A silent fallback hides the real defect and ships a quietly worse product.",
      "tags": ["errors", "observability", "loud-fail"]
    },
    {
      "id": "knowledge_seed_classify_failure",
      "kind": "best_practice",
      "title": "Classify a failure before reacting to it",
      "body": "Distinguish a genuine external limit (a provider rate limit) from a self-inflicted contract gap (oversized input or output, a schema or field-name mismatch) and fix the actual cause. Only a true external rate limit justifies a retry.",
      "tags": ["llm", "retry", "failure-class", "contract"]
    },
    {
      "id": "knowledge_seed_strategy_over_scalars",
      "kind": "best_practice",
      "title": "Fix the strategy before the numbers",
      "body": "When output is weak, examine the sequence and strategy first (rotation, ordering, composition, kit), because bigger magnitudes rarely fix a broken approach. Tune scalars only once the structure is right.",
      "tags": ["balance", "rotation", "strategy", "tuning"]
    },
    {
      "id": "knowledge_seed_blind_retry",
      "kind": "anti_pattern",
      "title": "Blind retry on provider errors",
      "body": "Retrying an LLM call on rate-limit or token-max without fixing the oversized input/output or the contract that caused it. Every retry that is not for a true external rate limit is a masked bug being papered over.",
      "tags": ["llm", "retry", "rate-limit", "token-max"]
    },
    {
      "id": "knowledge_seed_scalar_inflation",
      "kind": "anti_pattern",
      "title": "Scalar inflation instead of structural fix",
      "body": "Multiplying all damage or values to hit a target number instead of asking whether the kit, the rotation, or the structure is what is actually wrong. It treats a design problem as an arithmetic one.",
      "tags": ["balance", "multipliers", "shallow", "tuning"]
    },
    {
      "id": "knowledge_seed_spam_one_action",
      "kind": "anti_pattern",
      "title": "Spamming one action with no rotation",
      "body": "Repeating a single move with no rotation or strategy, optimizing one local number while the overall approach goes unexamined. The symptom of never asking whether the strategy itself is right.",
      "tags": ["rotation", "strategy", "shallow", "gameplay"]
    },
    {
      "id": "knowledge_seed_band_aid",
      "kind": "anti_pattern",
      "title": "Band-aid patch over a symptom",
      "body": "Wrapping a guard around a symptom so it stops showing, leaving the root cause live to resurface elsewhere. Defers the real fix and adds debt that compounds.",
      "tags": ["bandaid", "patch", "debt", "symptom"]
    },
    {
      "id": "knowledge_seed_hard_coding",
      "kind": "anti_pattern",
      "title": "Hard-coding values, paths, or vocabulary",
      "body": "Baking values, file paths, or domain-specific vocabulary into code or schema so it works for one case and breaks for the next. Derive these from config, data, or computation instead.",
      "tags": ["hard-coding", "config", "generality"]
    },
    {
      "id": "knowledge_seed_silent_fallback",
      "kind": "anti_pattern",
      "title": "Silent fallback that hides failure",
      "body": "Swapping in a degraded or canned path on error without surfacing it, so a real defect ships looking healthy. For design generation there is no algorithmic fallback; the only fallback is another model.",
      "tags": ["fallback", "silent", "loud-fail", "llm"]
    },
    {
      "id": "knowledge_seed_budget_quality_away",
      "kind": "anti_pattern",
      "title": "Budgeting quality away for time or tokens",
      "body": "Trading correctness for time, effort, or token savings. Shortcuts taken to fit a budget become the next bug; quality is the constraint, not the variable.",
      "tags": ["budget", "shortcut", "quality"]
    },
    {
      "id": "knowledge_seed_copy_paste",
      "kind": "anti_pattern",
      "title": "Copy-paste duplication instead of abstraction",
      "body": "Duplicating logic across call sites instead of extracting a shared abstraction, which guarantees the copies drift apart over time. The opposite of a single source of truth.",
      "tags": ["duplication", "abstraction", "drift"]
    },
    {
      "id": "knowledge_seed_no_hand_waving",
      "kind": "principle",
      "title": "No hand-waving",
      "body": "Every design decision is fully reasoned and concrete, with a real mechanism behind it. No vague gesture stands in for an actual design.",
      "tags": ["principle", "rigor", "no-handwaving"]
    },
    {
      "id": "knowledge_seed_no_band_aids",
      "kind": "principle",
      "title": "No band-aids",
      "body": "Only proper, root-cause designs ship. Never a temporary patch that defers the real fix; if the proper design means rebuilding, rebuild.",
      "tags": ["principle", "no-bandaid", "proper-fix"]
    },
    {
      "id": "knowledge_seed_no_budgeting",
      "kind": "principle",
      "title": "Do not budget time, effort, or tokens against quality",
      "body": "Quality is the binding constraint. Never compromise the design to save effort or tokens, and never include time or effort estimates in the work.",
      "tags": ["principle", "quality", "no-budget"]
    },
    {
      "id": "knowledge_seed_think_long_term",
      "kind": "principle",
      "title": "Always think long-term",
      "body": "Design for where the system is going, not just the immediate ask. Favor choices that stay correct and clean as the system grows.",
      "tags": ["principle", "long-term", "scalability"]
    },
    {
      "id": "knowledge_seed_automation",
      "kind": "principle",
      "title": "Automate over manual steps",
      "body": "Prefer a repeatable automated mechanism over a manual step. If something is done more than once, the harness should do it.",
      "tags": ["principle", "automation"]
    },
    {
      "id": "knowledge_seed_abstraction",
      "kind": "principle",
      "title": "Abstract recurring structure",
      "body": "Factor recurring structure into clean, reusable abstractions instead of one-off implementations. Abstraction is how the system stays small as it grows.",
      "tags": ["principle", "abstraction", "reuse"]
    },
    {
      "id": "knowledge_seed_no_hard_coding",
      "kind": "principle",
      "title": "No hard-coding",
      "body": "Values, paths, and vocabularies come from config, data, or derivation, never literals baked into logic. Hard-coding is how a general system silently becomes a single-case one.",
      "tags": ["principle", "no-hard-coding", "config"]
    },
    {
      "id": "knowledge_seed_verify_effect_not_declaration",
      "kind": "lesson",
      "title": "Verify effect, not declaration",
      "body": "A gate that checks declarations, or that an artifact merely exists, will pass while the behavior is broken. A pipeline once had a system-interaction map, a requirements-traceability matrix, and a value-impact map, yet a new upgrade that lowered an ability's cooldown changed nothing on screen or in combat, because every check verified what the code DECLARED and never what it DID. Ground completeness checks in the actual dataflow (the reads, writes, and calls in the code) so they fail when an effect is missing, not only when a declaration is.",
      "tags": ["completeness", "dataflow", "verification", "gate", "lesson"]
    },
    {
      "id": "knowledge_seed_derived_value_one_accessor",
      "kind": "lesson",
      "title": "A value that varies needs one write-point and one accessor",
      "body": "When a base value can be modified (a talent, a buff, a difficulty scalar), compute the effective value as base-combined-with-modifiers in exactly ONE accessor that every consumer reads, and land every modifier at ONE write-point. The talent-cooldown bug had both halves broken: the spent point landed where the resolver never read it, so the counter never moved, and most consumers still read the base field, so the menu, tooltip, hotbar sweep, and combat engine all ignored the change. Without a single accessor, 'make this value adjustable' silently means 'remember to adjust it in every place that reads it'.",
      "tags": ["single-source-of-truth", "derived-value", "accessor", "propagation", "lesson"]
    },
    {
      "id": "knowledge_seed_payload_validated_not_used",
      "kind": "lesson",
      "title": "A validated payload is not a used payload",
      "body": "Validating that a message carries the right KEYS proves the schema, not that any consumer reads them. An event bus that checked payload key names still let a value travel to subscribers that never consumed it. Completeness for a piece of data means tracing it to a consumer that acts on it, not confirming it was published in the correct shape.",
      "tags": ["events", "payload", "consumer", "dataflow", "lesson"]
    },
    {
      "id": "knowledge_seed_map_value_level_code_derived",
      "kind": "lesson",
      "title": "A completeness map must be value-level, layer-complete, and code-derived",
      "body": "A system-to-system wiring map is too coarse to catch a single value that fails to propagate; the map must track the individual DATA ELEMENT (one number, one name) across every layer it touches: visuals, engine, network, cache, storage, plus the animation, sound, and VFX timings that must rescale with it. And it must be DERIVED from the code (an AST walk), never declared, because a declaration is exactly the thing the code can silently diverge from.",
      "tags": ["completeness-map", "value-level", "code-derived", "ast", "lesson"]
    },
    {
      "id": "knowledge_seed_hand_over_impact_set",
      "kind": "lesson",
      "title": "Hand the implementer the full impact set",
      "body": "Do not require a person to enumerate every place a change must touch; that is just coding by dictation, and they will miss a nook. Compute the impact set for a change from the map and present it, so adding or removing a feature automatically surfaces every site that must follow: the counter, the description, the UI timer, the engine, the replication, the persistence. The tool's job is to make completeness the default, not a feat of memory.",
      "tags": ["impact-set", "change-completeness", "ergonomics", "lesson"]
    },
    {
      "id": "knowledge_seed_instruction_is_not_enforcement",
      "kind": "lesson",
      "title": "A standing instruction is not enforcement",
      "body": "Telling an assistant 'lock all the enums' or 'always use the canonical types' is a declaration of intent, and intent drifts; types and names kept slipping out of band despite many such reminders. The only thing that actually stops drift is a mechanical check that reads the code and rejects any literal used as an identity that is not in the canonical set. If a rule matters, encode it as an enforced check, not a reminder.",
      "tags": ["enforcement", "enum-lock", "drift", "identity", "lesson"]
    },
    {
      "id": "knowledge_seed_identities_fan_out_like_values",
      "kind": "lesson",
      "title": "Identities fan out like values",
      "body": "A name, enum member, or key (a status type, an ability id, an event name) propagates through code exactly as a numeric value does and breaks the same way: it must live in ONE canonical set, be spelled only from that set, and reach every consumer. Drift hides in an `if x == \"freeze\"` comparison just as readily as in a `match` block, so the check must cover every comparison and assignment form, not just one syntax.",
      "tags": ["identity", "enum", "canonical-set", "drift", "lesson"]
    },
    {
      "id": "knowledge_seed_completeness_spans_domains",
      "kind": "lesson",
      "title": "Completeness spans every domain and substrate",
      "body": "A change is not complete when the server is correct; the same value usually lives across the server, the client, the protocol between them, config, docs, schema, and stored data, and a fix that lands in one domain is invisible in the others. The talent change was applied server-side and never mirrored to the client, so the player saw nothing. Treat server/client (authoritative/replicated) as a first-class axis of the map, and check that an authoritative change is both replicated and read on the other side.",
      "tags": ["server-client", "boundary", "replication", "domains", "lesson"]
    },
    {
      "id": "knowledge_seed_static_vs_dynamic_completeness",
      "kind": "lesson",
      "title": "Static and dynamic substrates need different completeness",
      "body": "Things you can read statically (server code, client code, the protocol, config, schema, docs, text and data files) need REFERENCE completeness: every site that mentions the thing is consistent. Things that hold runtime bytes (database rows, caches, live processes, save, dump, and serialized files) need MIGRATION or INVALIDATION completeness: existing data must be migrated or invalidated when the shape changes. A map that only checks source files will pass while stale saved data still carries the old value.",
      "tags": ["static", "dynamic", "migration", "cache", "persistence", "lesson"]
    },
    {
      "id": "knowledge_seed_declared_must_be_read",
      "kind": "lesson",
      "title": "A declared knob nothing reads is a promise the code never keeps",
      "body": "An exported setting, a constant, or a config field that is declared but read by no code is dead: it looks like a control a designer can turn, yet turning it changes nothing. An audit of a status-effect system found six such knobs (a max-stacks cap, an armor-ignore flag, three crowd-control toggles) wired to nobody, plus a payload field written into a dict that no consumer ever read back. Treat 'declared but unread anywhere in the project' as a completeness gap of its own, distinct from value propagation: propagation asks whether a read sees modifiers, this asks whether the declaration has any read at all.",
      "tags": ["dead-code", "declared-not-read", "completeness", "lifecycle", "audit", "lesson"]
    },
    {
      "id": "knowledge_seed_dead_export",
      "kind": "anti_pattern",
      "title": "Exported setting that no code reads",
      "body": "Adding an exported field, a constant, or a config knob (or writing a key into a payload) and never reading it anywhere, so the control is inert. It silently widens the apparent design surface while doing nothing and misleads every later reader into believing the behavior exists. Either wire the declaration to a real read or delete it.",
      "tags": ["dead-code", "export", "config", "anti-pattern", "audit"]
    },
    {
      "id": "knowledge_seed_boundary_enforced_in_code",
      "kind": "lesson",
      "title": "A trust boundary lives in code, never in a comment",
      "body": "A save/replication system documented a mirror contract in a comment block ('mutators no-op on the client so the mirror never drifts from server authority'), yet not one of its public mutators actually called the authority guard, so a client could mutate and persist divergent state. A comment describing an invariant is not the invariant; only a guard the code executes enforces it. When a function must run on the authoritative side only, gate it with an explicit authority check at the top, and verify that gate mechanically rather than trusting the prose beside it.",
      "tags": ["authority", "server-client", "trust-boundary", "enforcement", "replication", "audit", "lesson"]
    },
    {
      "id": "knowledge_seed_ungated_mutator",
      "kind": "anti_pattern",
      "title": "Authoritative mutator with no authority guard",
      "body": "A state-mutating method that must run only on the server or owner but carries no is-authority guard, so any peer can call it and corrupt shared or persisted state. Doubly dangerous when a nearby comment claims the guard exists: the documentation reassures while the code omits it.",
      "tags": ["authority", "mutator", "replication", "anti-pattern", "audit"]
    },
    {
      "id": "knowledge_seed_every_read_surface_is_a_consumer",
      "kind": "lesson",
      "title": "Every display and gate surface is a consumer of the effective value",
      "body": "When a value can be modified, the engine is not its only consumer: the tooltip, the range indicator, the affordability grey-out, the charge counter, and the slot label all read it too, and each must read the RESOLVED value, not the base. An audit found a HUD where the out-of-range dim, the cost gate, and the tooltip each read base fields, so a talent that extended range or cut cost changed the cast while the UI kept showing and gating on the old numbers. A read-only surface that shows or gates on a stale base value is as broken as an engine that ignores the modifier.",
      "tags": ["accessor", "hud", "tooltip", "consumer", "propagation", "audit", "lesson"]
    },
    {
      "id": "knowledge_seed_closed_match_must_be_exhaustive",
      "kind": "lesson",
      "title": "A closed match over an identity must be exhaustive or fail loud",
      "body": "Dispatching on an identity (an effect kind, a status type, a message name) with a catch-all default that silently returns nothing means every member added later is dropped without a trace. An audit found an ability dispatcher whose match handled a fixed handful of effect kinds and let any unrecognized kind fall through to a silent failure. Either enumerate every canonical member, or make the default branch fail loud so a new, unhandled member is impossible to ship unnoticed.",
      "tags": ["exhaustiveness", "match", "identity", "loud-fail", "dispatch", "audit", "lesson"]
    },
    {
      "id": "knowledge_seed_two_sets_that_must_agree_are_one",
      "kind": "lesson",
      "title": "If two collections must stay in sync, they are one collection",
      "body": "An audit found two separate constant sets enumerating the same crowd-control kinds for two different purposes; keeping both is a standing invitation for one to gain a member the other lacks. Whenever two lists, maps, or sets are required to hold the same membership, derive one from the other or build both from a single canonical source. Duplicated membership is drift waiting to happen, the identity-level form of copy-paste.",
      "tags": ["single-source-of-truth", "identity", "duplication", "drift", "audit", "lesson"]
    },
    {
      "id": "knowledge_seed_signal_needs_listener_and_clear_contract",
      "kind": "lesson",
      "title": "An emitted signal needs a listener and an unambiguous contract",
      "body": "A signal emitted but connected by nobody is a dead wire; a signal reused for a second meaning by emitting it with empty or placeholder arguments overloads its contract so listeners cannot tell the two cases apart. An audit found a save event re-emitted with a blank id purely to nudge a view repaint, conflating 'a specific thing was saved' with 'the view changed'. Give each distinct event its own signal, and check that every emitted signal has at least one connection.",
      "tags": ["signals", "emit-connect", "contract", "events", "audit", "lesson"]
    },
    {
      "id": "knowledge_seed_audit_findings_need_verification",
      "kind": "principle",
      "title": "Treat every audit finding as a hypothesis until traced to live code",
      "body": "An automated or model-driven audit reads code in isolation and reports symptoms, so a large fraction of its findings are false positives, stale, or design opinions stated as defects. Before acting on any finding, trace it to the actual dataflow and runtime semantics and classify it true or false; fold only the verified ones into knowledge, and record the rejected ones so the same noise is not re-raised next time. An unverified finding acted on as fact is how a 'fix' breaks working code.",
      "tags": ["audit", "verification", "false-positive", "discipline", "principle"]
    },
    {
      "id": "knowledge_seed_common_audit_false_positives",
      "kind": "lesson",
      "title": "Common audit false positives: runtime handles, spec-file metadata, intentional markers",
      "body": "Recurring non-issues an audit raises that are not bugs: a runtime instance handle (an opaque per-instance id) flagged for not following a human-readable naming scheme, when the opaque handle is the correct runtime key; cross-system metadata (provides/reads-from wiring, a system's type) reported as missing from a code template, when that metadata legitimately lives in the spec the template is generated from, not in the template; an underscore-prefixed unused parameter called dead, when the prefix is the language's deliberate 'intentionally unused' marker; and a signal or field reuse flagged as a bug when a comment documents it as intended behavior. Verify the substrate and the convention before treating any of these as a defect.",
      "tags": ["audit", "false-positive", "runtime-handle", "spec-metadata", "convention", "lesson"]
    },
    {
      "id": "knowledge_seed_supervision_is_staged",
      "kind": "lesson",
      "title": "Supervision is staged across the whole task, not one gate",
      "body": "Watching an agent's work for drift from intent cannot be a single checkpoint, because divergence enters at several distinct points across a task: the request is mis- or under-understood before any work starts; the request pulls against what the project already is (its prior designs, settled decisions, stated focus); the proposed approach is a band-aid that misreads the root cause; the implementation quietly diverges from the approach that was agreed; and the lesson from this task is never captured, so the same class of drift recurs next time. A reviewer placed only at the approach-versus-vision moment is blind to the other four entry points, and a check that fires once is blind to recurrence. Stage the supervision so each entry point has its own check, make each one surface its finding loudly, and let a later stage re-catch what an earlier one — or a human correction — missed. Drift control is cumulative across stages and across repetitions, never caught-once-and-done.",
      "tags": ["supervision", "drift", "staged", "review", "intent", "lesson"]
    },
    {
      "id": "knowledge_seed_saddle_identity",
      "kind": "fact",
      "title": "'saddle' is this LLM harness itself",
      "body": "saddle is the name of this system: an agentic LLM harness — a supervisory layer wrapping Claude (and a multi-provider LLM pool) and bound to one focus project. When a user says 'saddle' with no other qualifier, or refers to it in the context of LLMs, agents, or this assistant, they mean THIS harness — never any external product, protocol, or company that happens to share the name. saddle exists because a raw LLM drifts and does not learn between sessions; it is the part that remembers and enforces, deterministically. It intakes each prompt into a tracked, itemized todo list; gates every code mutation against standing doctrine before it lands (blocking cross-project deletes, unclassified removals, straying outside the focus project); maps and audits the codebase for value-drift and half-wired work; and runs a project-scoped agentic chat like Claude Code itself.",
      "tags": ["saddle", "identity", "self", "harness", "agent", "llm", "what-is-saddle"]
    },
    {
      "id": "knowledge_seed_memory_system",
      "kind": "fact",
      "title": "How saddle's long-term memory (the DKB) works",
      "body": "saddle's persistent memory is the Design Knowledge Base (DKB): one semantically-searchable store holding both design wisdom (best_practice, anti_pattern, lesson, principle) and plain memory (fact = a durable curated truth like identity or a stable invariant; reference = a cached file/web lookup). Retrieval is hybrid — a keyword index (FTS5) and a semantic vector index (sqlite-vec) queried together and fused with Reciprocal Rank Fusion — so an exact term and a paraphrase both surface; semantic search does NOT subsume keyword search, which is why both run. Knowledge is retrieved on demand (top-k, length-bounded, fresh per turn — see saddle.recall), never prepended wholesale, so context cannot balloon. Two tiers by an entry's durable bit: durable knowledge is retired-never-deleted; the cache tier (references) is reconstructible and therefore bounded and evictable — coldest-and-expired rows are dropped so storage never grows without bound. Use memory instead of re-reading the same files or re-searching the web every session.",
      "tags": ["saddle", "memory", "dkb", "recall", "cache", "semantic", "fact", "how-memory-works"]
    },
    {
      "id": "knowledge_seed_bounded_storage_cleanup",
      "kind": "principle",
      "title": "Any data store needs a bounded cleanup process",
      "body": "A store that only ever grows is a future failure: it bloats, slows, and eventually breaks. Every store of accumulating data must define, up front, how it stays bounded — a cap, a TTL, or an eviction policy — and that cleanup must be loud about what it removed (no silent truncation). The exception that proves the rule is reconstructible data: a cache you can rebuild from its source may be deleted to stay bounded without losing anything, which is exactly why a cache tier is the right home for evictable data while irreproducible knowledge is retired, never deleted.",
      "tags": ["storage", "cleanup", "eviction", "cache", "retention", "bounded", "principle"]
    },
    {
      "id": "knowledge_seed_memory_retrieved_not_prepended",
      "kind": "lesson",
      "title": "Long-term memory is retrieved, never prepended wholesale",
      "body": "Giving a model its memory by pasting standing facts in front of every prompt does not scale: the preamble grows with every new fact, so the context balloons exactly as the memory accumulates, and most of it is irrelevant to the turn at hand. Instead retrieve: query the memory for the few entries relevant to THIS prompt (semantic plus keyword), inject only the top-k, length-bounded, and recompute fresh each turn. Then memory can grow forever while what any single turn carries stays small and on-topic. Prepend-everything is the anti-pattern; query-scoped top-k retrieval (RAG) is the fix.",
      "tags": ["memory", "retrieval", "rag", "prepend", "context", "bounded", "lesson"]
    }
  ]
}
